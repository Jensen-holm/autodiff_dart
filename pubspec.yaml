name: autodiff_dart
description: automatic differentiation library for Dart inspired by micrograd
version: 1.0.1
repository: https://github.com/Jensen-holm/autodiff_dart

environment:
  sdk: ^3.4.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
